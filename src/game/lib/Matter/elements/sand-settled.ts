import { MatterType } from '../_Matter-types.ts'
import type { ElementDef } from '../elements.ts'

const def: ElementDef = {
  id: MatterType.SAND_SETTLED,
  name: 'Sand (Settled)',
  passive: true,
}

export default def
