import { EMPTY } from '../_Matter-types.ts'
import type { ElementDef } from '../elements.ts'

const def: ElementDef = {
  id: EMPTY,
  name: 'Empty',
  passive: true,
  lavaImmune: true,
  acidImmune: true,
}

export default def
