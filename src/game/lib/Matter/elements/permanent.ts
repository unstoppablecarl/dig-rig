import { PERMANENT } from '../_Matter-types.ts'
import type { ElementDef } from '../elements.ts'

const def: ElementDef = {
  id: PERMANENT,
  name: 'Permanent',
  passive: true,
  lavaImmune: true,
}

export default def
